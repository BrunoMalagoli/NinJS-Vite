import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
	colors: {
		primaryYellow: '#F8D859',
		primaryBG: '#16191C',
		primaryColor: '#F3F4F6',
		secondaryColor: '#C1C1C1'
	}
});

export default theme;
